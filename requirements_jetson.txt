# MediFlow Kiosk - Jetson Orin Nano application dependencies
# Verified on L4T R36.4.7 / Python 3.10.12. Revalidate these pins on a new L4T release.

# Web
Flask==3.1.3
Flask-Cors==6.0.2
python-dotenv==1.2.2
requests==2.32.5

# Numeric / image
numpy==1.26.4
Pillow==12.1.1

# Detection / model utilities
mediapipe==0.10.18
grad-cam==1.5.5

# Reports
qrcode[pil]==8.2
reportlab==4.4.10

# Do not add torch, torchvision, or OpenCV here. scripts/install_jetson.sh validates
# JetPack-compatible torch/torchvision builds and the existing cv2 installation.
